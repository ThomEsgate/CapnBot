const { Command } = require('discord-akairo');

class Horny extends Command {
    constructor() {
        super('horny', {
            //if a sentence contains rude word!!
            regex: /[\s\S]*(cock|ass|willy|cunt|horny|yiff|penis|porn|hentai)[\s\S]* *$/
        });
    }

    exec(message, args) {
        message.channel.send(message.content + ' idk how to react to stuff yet');
    }
}

module.exports = Horny;
